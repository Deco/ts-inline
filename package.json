{
  "name": "ts-inline",
  "version": "0.1.0",
  "description": "TypeScript compiler extension for better debugging messages",
  "author": {
    "name": "Declan White",
    "email": "ts-inline@qn.design"
  },
  "license": "MIT",
  "files": [
    "dist"
  ],
  "scripts": {
    "prebuild": "rimraf dist",
    "build": "tsc",
    "prepare": "npm run build",
    "test": "jest"
  },
  "devDependencies": {
    "@types/jest": "^26.0.23",
    "jest": "^26.6.3",
    "rimraf": "^3.0.2",
    "ts-jest": "^26.5.6",
    "ts-node": "^9.1.1",
    "typescript": "^4.2.4"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node"
  },
  "directories": {
    "test": "tests"
  },
  "preferDev": true,
  "dependencies": {
    "@wessberg/ts-clone-node": "^0.3.19",
    "browser-util-inspect": "^0.2.0",
    "ts-transformer-testing-library": "file:../ts-transformer-testing-library"
  }
}
